# 开发日志

## 2026-05-23

- 读取 `GOAL.md` 和 `REQUIREMENT.md`，确认第一阶段目标是搭建 ESP32-S3 N16R8 Rust 嵌入式项目架构并跑通 `hello world`，且所有项目修改需要维护本日志。
- 检查工作区，当前目录只有 `GOAL.md` 和 `REQUIREMENT.md`，不是 git 仓库。
- 调研最新官方文档和 crate 信息：
  - `esp-hal` 当前最新 crate 为 `1.1.1`，ESP32-S3 target 为 `xtensa-esp32s3-none-elf`。
  - Rust on ESP Book 的 async 章节说明 `esp-hal` 提供 blocking/async API，Embassy 集成当前由 `esp-rtos` 提供。
  - `esp-rtos` 当前文档版本为 `0.3.0`，启用 `embassy` feature 后使用 `#[esp_rtos::main]` 创建 Embassy executor。
  - `esp-hal-embassy` 已被合并/迁移到 `esp-rtos`，不作为新项目首选。
- 检查本机工具链：
  - `rustc 1.95.0`
  - `cargo 1.95.0`
  - 已安装 target 只有 `x86_64-unknown-linux-gnu`
  - 未安装 `espflash` 和 `esp-generate`
- 尝试执行 `rustup target add xtensa-esp32s3-none-elf`，失败：stable 工具链没有该 target 的预编译 artifacts。该问题符合 ESP32-S3 Xtensa 目标需要 ESP Rust 工具链的预期，后续需要通过 `espup` 安装 `esp` toolchain。
- 新增项目骨架：
  - `Cargo.toml`
  - `.cargo/config.toml`
  - `rust-toolchain.toml`
  - `build.rs`
  - `src/main.rs`
  - `README.md`
- `src/main.rs` 使用 `esp-hal` 初始化 ESP32-S3，启动 `esp-rtos` 调度器，并通过 `esp-println` 与 `log` 每秒输出 hello world tick。
- 安装 `espup v0.17.1`，随后执行 `espup install` 安装 ESP Rust Xtensa 工具链。安装完成后需要在 shell 中加载 `/home/pcsensor/export-esp.sh`。
- 加载 ESP 工具链环境后执行 `cargo check`，依赖下载和编译正常进入项目检查阶段，但失败于 `src/main.rs` 的 `esp_println::init_logger_from_env` 导入路径。`esp-println 0.17.0` 中该函数位于 `esp_println::logger::init_logger_from_env`。
- 修正 `src/main.rs` 的 logger 初始化函数导入路径。
- 重新执行 `cargo check`，通过。
- 执行 `cargo check --release`，通过。
- 执行 `cargo build --release`，通过，release 固件可成功链接生成。
- 安装 `espflash v4.4.0`。
- 执行 `cargo run --release` 尝试烧录和监控，构建阶段通过，runner 调用 `espflash flash --monitor target/xtensa-esp32s3-none-elf/release/esp32s3-n16r8-rust`。`espflash` 检测到 `/dev/ttyACM0` 后连接失败：`Failed to open serial port /dev/ttyACM0`。
- 继续检查串口状态，`/dev/ttyACM0` 已不存在，`/dev` 下没有 `ttyACM*` 或 `ttyUSB*` 设备；当前用户组为 `pcsensor nobody`，不在常见串口权限组中。第一阶段的软件项目构建已跑通，实际烧录 hello world 还需要接入开发板并解决串口设备/权限问题。
- 最终确认工具和产物：
  - `espup 0.17.1`
  - `espflash 4.4.0`
  - `rustup toolchain list` 显示 `esp (active)`
  - release ELF 产物为 `target/xtensa-esp32s3-none-elf/release/esp32s3-n16r8-rust`，大小 269656 bytes。
- 用户在未加载 `/home/pcsensor/export-esp.sh` 的 shell 中执行 `cargo run --release`，构建失败：`linker xtensa-esp32s3-elf-gcc not found`。确认未 source 时找不到 linker，source 后 linker 位于 `/home/pcsensor/.rustup/toolchains/esp/xtensa-esp-elf/esp-15.2.0_20250920/xtensa-esp-elf/bin/xtensa-esp32s3-elf-gcc`。
- 加载 `/home/pcsensor/export-esp.sh` 后重新执行 `cargo run --release`，linker 问题消失，release 构建完成并进入 `espflash`。烧录仍失败于串口打开：`Failed to open serial port /dev/ttyACM0`。随后检查时 `/dev/ttyACM0` 已不存在。
- 新增 `scripts/run-release.sh`，自动加载 ESP 工具链环境后执行 `cargo run --release`，避免新 shell 中忘记 source 导致 linker 缺失。
- 设置 `scripts/run-release.sh` 为可执行文件。
- 用户修复串口临时权限后执行 `./scripts/run-release.sh`，`espflash` 成功连接 `/dev/ttyACM0` 并进入 flash stub，但失败于固件缺少 ESP-IDF App Descriptor。错误提示需要加入 `esp_bootloader_esp_idf::esp_app_desc!()`。
- 按 `esp-bootloader-esp-idf 0.5.0` 文档新增 `esp-bootloader-esp-idf` 依赖并启用 `esp32s3` feature，在 `src/main.rs` 顶层加入 `esp_bootloader_esp_idf::esp_app_desc!();`。
- 执行 `cargo check --release`，通过。
- 执行 `cargo run --release`，`espflash` 成功识别 ESP32-S3 revision v0.2、40 MHz crystal、16MB flash，并完成烧录。非交互监控环境中出现 `Failed to initialize input reader`，这是本次执行环境的 monitor 输入 reader 问题，烧录本身已完成。
- 使用 TTY 模式重新执行 `cargo run --release`，烧录完成并成功进入监控，串口输出：
  - `Hello world from ESP32-S3 N16R8!`
  - 每秒输出 `INFO - hello world tick`
- 第一阶段目标达成：ESP32-S3 N16R8 Rust 项目架构已搭建，`esp-hal` + `esp-rtos` + Embassy hello world 已成功构建、烧录并运行。
